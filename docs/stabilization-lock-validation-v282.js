import {getApp} from 'https://www.gstatic.com/firebasejs/10.12.5/firebase-app.js';
import {getAuth,onAuthStateChanged} from 'https://www.gstatic.com/firebasejs/10.12.5/firebase-auth.js';
import {getFirestore,doc,getDoc} from 'https://www.gstatic.com/firebasejs/10.12.5/firebase-firestore.js';
const auth=getAuth(getApp()),db=getFirestore(getApp());
const EDITABLE=['Draft','Revision Requested'];
let profile={role:'user'},activeId='',activeStatus='Draft',patched=false;
const $=id=>document.getElementById(id);
const canonical=s=>['Finance Processing','Financial Processing'].includes(s)?'Reimbursement Processing':(s||'Draft');
async function statusOf(id){try{const s=await getDoc(doc(db,'expenseReports',id));return s.exists()?canonical(s.data().status):'Draft'}catch(e){return 'Draft'}}
function setReadOnly(status){activeStatus=canonical(status);const locked=!!activeId&&!EDITABLE.includes(activeStatus)&&profile.role!=='admin',editor=$('editor');if(!editor)return;editor.dataset.readonly=locked?'true':'false';let b=$('editorReadonlyBanner');if(!b){b=document.createElement('div');b.id='editorReadonlyBanner';b.className='editor-readonly-banner';editor.prepend(b)}b.style.display=locked?'block':'none';b.innerHTML=locked?`<strong>Read-only report</strong><br>This report is ${activeStatus} and cannot be edited.`:'';editor.querySelectorAll('input,select,textarea').forEach(el=>el.disabled=locked)}
function validate(){const errors=[],bad=[],req=[['employee','Employee name'],['purpose','Business purpose'],['fromDate','Expense period from'],['toDate','Expense period to'],['destination','Destination']];document.querySelectorAll('.field-error').forEach(e=>e.classList.remove('field-error'));$('validationSummary')?.remove();req.forEach(([id,label])=>{const el=$(id);if(!el?.value?.trim()){errors.push(label+' is required.');bad.push(el)}});const from=$('fromDate')?.value,to=$('toDate')?.value;if(from&&to&&to<from){errors.push('To Date cannot be earlier than From Date.');bad.push($('toDate'))}const items=[...document.querySelectorAll('#items .item')];if(!items.length)errors.push('At least one expense item is required.');items.forEach((item,i)=>{const x=item.querySelectorAll('input'),date=x[0],desc=x[1],amount=x[2],fx=x[3];if(!date?.value){errors.push(`Item ${i+1}: date is required.`);bad.push(date)}if(!desc?.value?.trim()){errors.push(`Item ${i+1}: description is required.`);bad.push(desc)}if(!(Number(amount?.value)>0)){errors.push(`Item ${i+1}: amount must be greater than zero.`);bad.push(amount)}if(!(Number(fx?.value)>0)){errors.push(`Item ${i+1}: FX rate must be greater than zero.`);bad.push(fx)}if(date?.value&&from&&date.value<from)errors.push(`Item ${i+1}: date is before report period.`);if(date?.value&&to&&date.value>to)errors.push(`Item ${i+1}: date is after report period.`)});if(!errors.length)return true;bad.filter(Boolean).forEach(e=>e.classList.add('field-error'));const d=document.createElement('div');d.id='validationSummary';d.className='validation-summary';d.innerHTML='<strong>Please correct:</strong><br>'+[...new Set(errors)].map(e=>'• '+e).join('<br>');$('editor')?.prepend(d);alert([...new Set(errors)].join('\n'));return false}
function patch(){if(patched||!window.openReport||!window.newReport||!window.saveReport)return;patched=true;const oldOpen=window.openReport;window.openReport=async id=>{activeId=id;activeStatus=await statusOf(id);oldOpen(id);setTimeout(()=>setReadOnly(activeStatus),200)};const oldNew=window.newReport;window.newReport=(open=true)=>{activeId='';activeStatus='Draft';oldNew(open);setTimeout(()=>setReadOnly('Draft'),100)};const oldSave=window.saveReport;window.saveReport=async(status='Draft')=>{const next=canonical(status);if(activeId&&!EDITABLE.includes(activeStatus)&&profile.role!=='admin')return alert(`This report is ${activeStatus} and is read-only.`);if(next==='Submitted'&&!validate())return;return oldSave(next)};if(window.financeAction){const f=window.financeAction;window.financeAction=(id,status)=>f(id,canonical(status))}}
onAuthStateChanged(auth,async u=>{if(!u)return;try{const s=await getDoc(doc(db,'users',u.uid));profile=s.exists()?s.data():{role:'user'}}catch(e){}setTimeout(patch,1500)});setInterval(patch,1000);
