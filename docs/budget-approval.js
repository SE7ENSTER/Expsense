import { getApp } from 'https://www.gstatic.com/firebasejs/10.12.5/firebase-app.js';
import { getAuth,onAuthStateChanged } from 'https://www.gstatic.com/firebasejs/10.12.5/firebase-auth.js';
import { getFirestore,collection,doc,getDoc,getDocs,query,where,setDoc,serverTimestamp } from 'https://www.gstatic.com/firebasejs/10.12.5/firebase-firestore.js';
const auth=getAuth(getApp());const db=getFirestore(getApp());let me=null,profile={role:'user'},reports=[],budgets=[];const $=id=>document.getElementById(id);const money=n=>'HKD '+Number(n||0).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2});const total=r=>(r.items||[]).reduce((s,i)=>s+(+i.amount||0)*(+i.fx||0),0);const fy=d=>{const x=d?new Date(d):new Date();return 'FY'+(x.getMonth()>=10?x.getFullYear()+1:x.getFullYear())};const isAdmin=()=>profile.role==='admin';const isManager=()=>profile.role==='manager';const isFinance=()=>profile.role==='finance';const canReview=()=>isAdmin()||isManager()||isFinance();function reportFy(r){return r.fiscalYear||fy(r.meta?.fromDate)}function reportTotal(r){return +(r.totalHkd||total(r))}
const css=`#reportList.report-list{display:flex!important;flex-direction:column!important;gap:12px!important}#reportList .report-card{display:grid!important;grid-template-columns:135px minmax(0,1fr) 180px 95px!important;align-items:center!important;gap:16px!important;padding:18px 22px!important;border-radius:22px!important;min-height:92px!important}#reportList .report-card .pill{grid-column:1;grid-row:1 / span 3;justify-self:start;margin:0;font-size:13px;padding:8px 12px}#reportList .report-card h3{grid-column:2;margin:0!important;font-size:18px!important}#reportList .report-card p{grid-column:2;margin:2px 0!important}#reportList .report-card .small{font-size:13px!important}#reportList .report-card .row{grid-column:3;grid-row:1 / span 3;display:flex!important;flex-direction:column!important;align-items:flex-start!important;justify-content:center!important;gap:4px!important}#reportList .report-card .row strong{font-size:18px}.report-view-tag{grid-column:4;grid-row:1 / span 3;justify-self:end;background:#f2f7ff;color:#0b6bff;border:1px solid #cfe0ff;border-radius:999px;padding:8px 12px;font-weight:900;font-size:12px}.readonly-banner{padding:14px 18px;margin:0 0 16px;border:1px solid #cfe0ff;background:#f2f7ff;border-radius:18px;color:#164b8b;font-weight:850}.readonly-field{opacity:.68;pointer-events:none}#editReportBtn{background:#fff!important;color:#0b6bff!important;border:1px solid #cfe0ff!important;box-shadow:none!important}@media(max-width:760px){#reportList .report-card{grid-template-columns:1fr!important}#reportList .report-card .pill,#reportList .report-card h3,#reportList .report-card p,#reportList .report-card .row,.report-view-tag{grid-column:1!important;grid-row:auto!important;justify-self:start!important}}`;
const style=document.createElement('style');style.textContent=css;document.head.appendChild(style);
async function loadProfile(u){const s=await getDoc(doc(db,'users',u.uid));profile=s.exists()?s.data():{role:'user',email:u.email};if(!s.exists())await setDoc(doc(db,'users',u.uid),{email:u.email,role:'user',active:true,createdAt:serverTimestamp(),updatedAt:serverTimestamp()},{merge:true});$('tab-review')?.classList.toggle('hide',!canReview());$('tab-admin')?.classList.toggle('hide',!canReview());$('myRoleText')&&($('myRoleText').textContent=profile.role)}
async function reload(){if(!me)return;let rq=isAdmin()?collection(db,'expenseReports'):canReview()?query(collection(db,'expenseReports'),where('status','==','Submitted')):query(collection(db,'expenseReports'),where('userId','==',me.uid));reports=(await getDocs(rq)).docs.map(d=>({id:d.id,...d.data()}));let bq=canReview()?collection(db,'budgets'):query(collection(db,'budgets'),where('userId','==',me.uid));budgets=(await getDocs(bq)).docs.map(d=>({id:d.id,...d.data()}));renderBudget();renderReview();enhanceReportList()}
function budgetFor(uid,year){const b=budgets.find(x=>x.userId===uid&&x.fiscalYear===year);const amount=+(b?.budgetAmount||0);const mine=reports.filter(r=>r.userId===uid&&reportFy(r)===year);const used=mine.filter(r=>['Manager Approved','Finance Processing','Reimbursed','Closed'].includes(r.status)).reduce((s,r)=>s+reportTotal(r),0);const pending=mine.filter(r=>r.status==='Submitted').reduce((s,r)=>s+reportTotal(r),0);return{amount,used,pending,available:amount-used-pending}}
function renderBudget(){const box=$('budgetSummary');if(!box||!me)return;const y=fy();const b=budgetFor(me.uid,y);box.innerHTML='<div class="card"><div class="muted">'+y+' Budget</div><strong>'+money(b.amount)+'</strong></div><div class="card"><div class="muted">Used</div><strong>'+money(b.used)+'</strong></div><div class="card"><div class="muted">Pending</div><strong>'+money(b.pending)+'</strong></div><div class="card"><div class="muted">Available</div><strong>'+money(b.available)+'</strong></div>'}
function renderReview(){const tb=$('reviewReports');if(!tb)return;const rows=reports.filter(r=>r.status==='Submitted');tb.innerHTML=rows.map(r=>{const m=r.meta||{};const b=budgetFor(r.userId,reportFy(r));const after=b.available-reportTotal(r);return '<tr><td>'+(m.employee||r.userEmail||'-')+'</td><td>'+(m.purpose||'-')+'</td><td>'+reportFy(r)+'</td><td>'+money(reportTotal(r))+'</td><td>'+(after<0?'Over by '+money(Math.abs(after)):'After approval '+money(after))+'</td><td><span class="pill submitted">Submitted</span></td><td><button class="secondary" onclick="managerApprove(\''+r.id+'\')">Approve</button></td></tr>'}).join('')||'<tr><td colspan="7">No submitted reports waiting for review.</td></tr>'}
window.managerApprove=async id=>{if(!canReview())return alert('Manager/Admin only.');const r=reports.find(x=>x.id===id);await setDoc(doc(db,'expenseReports',id),{status:'Manager Approved',managerStatus:'Approved',managerId:me.uid,managerEmail:me.email,managerActionAt:serverTimestamp(),updatedAt:serverTimestamp(),fiscalYear:reportFy(r),totalHkd:reportTotal(r)},{merge:true});await reload();alert('Report approved.')};
function enhanceReportList(){const list=$('reportList');if(!list)return;list.querySelectorAll('.report-card').forEach(card=>{const pill=card.querySelector('.pill');const status=(pill?.textContent||'').trim();card.dataset.status=status;if(!card.querySelector('.report-view-tag')){const tag=document.createElement('span');tag.className='report-view-tag';tag.textContent=status==='Draft'?'Edit draft':'View';card.appendChild(tag)}})}
function ensureEditButton(){const actions=document.querySelector('#editor .toolbar .top-actions');if(!actions||$('editReportBtn'))return;const b=document.createElement('button');b.id='editReportBtn';b.className='secondary hide';b.type='button';b.textContent='Edit Report';b.onclick=()=>setReadOnly(false);actions.prepend(b)}
function setReadOnly(ro){ensureEditButton();const editor=$('editor');if(!editor)return;let banner=$('readonlyBanner');if(ro&&!banner){banner=document.createElement('div');banner.id='readonlyBanner';banner.className='readonly-banner';banner.textContent='This report has already been submitted. It is opened in view-only mode. Click Edit Report if you need to make changes.';editor.prepend(banner)}if(banner)banner.classList.toggle('hide',!ro);$('editReportBtn')?.classList.toggle('hide',!ro);editor.querySelectorAll('input,select,textarea').forEach(x=>{x.disabled=ro;x.classList.toggle('readonly-field',ro)});editor.querySelectorAll('.upload,.danger.full,.add').forEach(x=>x.classList.toggle('hide',ro));editor.querySelectorAll('.toolbar .top-actions button').forEach(b=>{if(b.id!=='editReportBtn'&&b.textContent!=='Export PDF')b.classList.toggle('hide',ro)})}
let lastStatus='';document.addEventListener('click',e=>{const card=e.target.closest('#reportList .report-card');if(card)lastStatus=card.dataset.status||card.querySelector('.pill')?.textContent.trim()||''},true);
const oldOpen=window.openReport;window.openReport=id=>{oldOpen(id);setTimeout(()=>setReadOnly(lastStatus&&lastStatus!=='Draft'),80)};const oldNew=window.newReport;window.newReport=(...a)=>{oldNew(...a);setTimeout(()=>setReadOnly(false),80)};const oldShow=window.showView;window.showView=view=>{oldShow(view);if(view==='dashboard')setTimeout(enhanceReportList,80)};
const obs=new MutationObserver(enhanceReportList);setTimeout(()=>{const list=$('reportList');if(list)obs.observe(list,{childList:true,subtree:true});enhanceReportList();ensureEditButton()},900);
window.refreshAdmin=reload;onAuthStateChanged(auth,async u=>{me=u;if(u){await loadProfile(u);setTimeout(reload,800)}});console.log('ExpenseFlow report list and view-only mode active');
